## Server config
# Postgres connection string (Neon/local Postgres)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# Session cookie signing secret (any long random string)
SESSION_SECRET=change-me-to-a-long-random-string

## Auth
# Available: local | replit | dev
# Default is "local"
AUTH_MODE=local

## Replit OIDC (only if AUTH_MODE=replit)
REPL_ID=your-repl-id
ISSUER_URL=https://replit.com/oidc

## Dev auth (only if AUTH_MODE=dev)
DEV_USER_ID=dev-user
DEV_EMAIL=dev@example.com
DEV_ROLE=admin

## OAuth Providers (optional)
# Base URL used to construct OAuth callback URLs. Example: http://localhost:5000 or https://mydomain.com
BASE_URL=http://localhost:5000

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

## SMTP (optional; used to send registration emails in local mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Comma-separated list of email addresses to receive event notifications
# Example: NOTIFY_EMAILS=admin@example.com,ops@example.com
NOTIFY_EMAILS=

## Optional S3 storage (images/audio/video)
# If `S3_BUCKET` is set, uploads will attempt to use S3. Provide AWS creds and region.
S3_BUCKET=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Optional base url for S3-hosted assets (e.g. https://cdn.example.com)
S3_BASE_URL=

# Optional
PORT=5000
